<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/content_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/send_to_friends"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@null"
            android:drawablePadding="10dp"
            android:drawableTop="@drawable/ic_share_with_friends"
            android:padding="20dp"
            android:text="@string/send_to_friends"
            android:textColor="#4a4a4a"
            android:textSize="15sp" />

        <Button
            android:id="@+id/share_in_timeline"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@null"
            android:drawablePadding="10dp"
            android:drawableTop="@drawable/ic_share_in_timeline"
            android:padding="20dp"
            android:text="@string/share_in_timeline"
            android:textColor="#4a4a4a"
            android:textSize="15sp" />

    </LinearLayout>

    <TextView
        android:id="@+id/cancel_share"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_rect_top_divider"
        android:gravity="center"
        android:padding="20dp"
        android:text="@string/cancel"
        android:textColor="#4A4A4A"
        android:textSize="15sp" />

</LinearLayout>